
const queries = {
    createUser : async (_ :any, {}:{})=>{
        console.log('hereer')
        return 'hello users opss'
    }
}

const mutations = {
}


export const resolvers = {queries, mutations}